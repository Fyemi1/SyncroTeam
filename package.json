{
    "name": "syncroteam-monorepo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "postinstall": "cd server && npx prisma generate"
    },
    "devDependencies": {
        "prisma": "^5.10.2"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}